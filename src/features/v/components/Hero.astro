---

---

<section class="world-hero" data-theme="v" aria-labelledby="v-hero-title">
  <p class="world-hero__eyebrow">Core Team @ V · vlang/v · laboratorio computacional</p>
  <h1 id="v-hero-title" class="world-hero__title">V Ecosystem — Fast, clear, scientific</h1>
  <p class="world-hero__lead">
    V for systems, VSL for numbers, VTL for tensors, RxV for time, setup-v for CI. Four years of verified contributions
    — shown as links to commits and repos, not badge theatre.
  </p>
  <div class="world-hero__badges" role="list" aria-label="Ecosystem highlights">
    <span class="world-hero__badge" role="listitem">MIT · vlang/v, vsl, vtl, setup-v</span>
    <span class="world-hero__badge" role="listitem">RxV · channels</span>
    <span class="world-hero__badge world-hero__badge--accent" role="listitem">accent #1e5a8a</span>
  </div>
</section>

<style>
  .world-hero {
    padding: var(--space-8) 0 1.2rem 0;
    border-bottom: 1px solid var(--color-border, #e9d8c3);
  }

  .world-hero__eyebrow {
    margin: 0 0 0.4rem 0;
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.07em;
    text-transform: uppercase;
    color: var(--theme-accent-strong, #16446a);
  }

  .world-hero__title {
    margin: 0 0 0.5rem 0;
    font-size: clamp(1.8rem, 4vw, 2.5rem);
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -0.03em;
    color: var(--color-deep-ink, #181512);
  }

  .world-hero__lead {
    margin: 0;
    max-width: 46rem;
    font-size: 1rem;
    line-height: 1.65;
    color: var(--color-moss-strong, #5a5e4f);
  }

  .world-hero__badges {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-top: 0.9rem;
  }

  .world-hero__badge {
    display: inline-flex;
    align-items: center;
    padding: 0.32rem 0.6rem;
    border-radius: 999px;
    font-size: 0.78rem;
    font-weight: 600;
    color: var(--color-deep-ink, #181512);
    background: var(--color-warm-white, #fffdf9);
    border: 1px solid var(--color-sand, #e9d8c3);
  }

  .world-hero__badge--accent {
    background: var(--theme-accent, #1e5a8a);
    color: white;
    border-color: var(--theme-accent-strong, #16446a);
  }
</style>
