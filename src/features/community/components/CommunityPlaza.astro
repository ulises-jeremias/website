---
import { SceneInspector, VisualNode, WorldDock } from '@/shared/components/visual';
import '@/shared/components/visual/visual.css';
import ContributionExplorer from './ContributionExplorer.astro';
import {
  activeWorkshopProjects,
  communityMeta,
  ecosystemLabels,
  incubatingProjects,
  moderationItems,
  supportChannels,
} from '../data/index.js';

const nodes = [
  {
    id: 'discord',
    name: 'Discord',
    summary: 'Live questions and coordination across Digital Nest projects.',
    areas: ['talk', 'coordinate', 'optional'],
    ecosystem: 'workshop',
    href: communityMeta.discordInviteUrl,
    kind: 'discord' as const,
  },
  ...activeWorkshopProjects.map((p) => ({
    id: p.id,
    name: p.name,
    summary: p.summary,
    areas: p.contributionAreas.slice(0, 4),
    ecosystem: ecosystemLabels[p.ecosystem],
    href: p.worldPath ?? (p.repo ? `https://github.com/${p.repo}` : (p.website ?? '#')),
    kind: 'project' as const,
  })),
];

const defaultNode = nodes.find((n) => n.id === 'agent-toolkit') ?? nodes[1] ?? nodes[0]!;
const nodesJson = JSON.stringify(nodes);
---

<section
  class="cm-plaza"
  aria-labelledby="cm-plaza-title"
  data-cm-plaza
  data-nodes={nodesJson}
  data-testid="community-plaza"
>
  <header class="cm-plaza__header">
    <div class="cm-plaza__header-copy">
      <p class="cm-plaza__eyebrow">Digital Nest · shared workshop</p>
      <h1 id="cm-plaza-title" class="cm-plaza__title">Digital Nest Community</h1>
      <p class="cm-plaza__lede">
        One Discord server hosts every project in this atlas. The plaza below maps its five neighborhoods — Personal DX,
        agentic tooling, Create Awesome, the V ecosystem, and incubating work — pick one to see what people build there
        and where to jump in.
      </p>
    </div>
    <aside class="cm-plaza__header-note" aria-label="How to read the community plaza">
      <p class="cm-plaza__kicker">Map legend</p>
      <p>
        The plaza is a route map, not a member counter. Choose a station to inspect its work, then use GitHub for the
        durable trail.
      </p>
      <a href="#find-something">Find a contribution path ↘</a>
    </aside>
  </header>

  <ul class="cm-plaza__principles" aria-label="Workshop principles">
    <li>One Discord · many projects</li>
    <li>Cross-pollination</li>
    <li>Talk in Discord · trail on GitHub</li>
  </ul>

  <div class="cm-plaza__discord" data-cm-discord>
    <p class="cm-plaza__kicker">Start here · shared Discord</p>
    <a
      class="cm-plaza__discord-cta"
      href={communityMeta.discordInviteUrl}
      target="_blank"
      rel="noopener noreferrer"
      aria-label="Join the Digital Nest Discord (opens in a new tab)"
    >
      Join the Discord
    </a>
    <span>{communityMeta.communityDistinctNote}</span>
  </div>

  <div class="vf-scene cm-plaza__scene">
    <div class="vf-stage cm-plaza__stage">
      <svg class="cm-plaza__svg" viewBox="0 0 720 280" role="img" aria-label="Community plaza with world clusters">
        <defs>
          <radialGradient id="cm-plaza-ground" cx="50%" cy="55%" r="55%">
            <stop offset="0%" stop-color="#1e293b" stop-opacity="0.9"></stop>
            <stop offset="100%" stop-color="#020617" stop-opacity="0"></stop>
          </radialGradient>
        </defs>
        <rect width="720" height="280" fill="url(#cm-plaza-ground)"></rect>
        <!-- Plaza floor / clusters -->
        <ellipse
          cx="360"
          cy="150"
          rx="210"
          ry="70"
          fill="color-mix(in srgb, #0f172a 80%, transparent)"
          stroke="color-mix(in srgb, var(--nest-cyan) 35%, transparent)"></ellipse>
        <g data-cm-cluster="dx" opacity="0.9">
          <rect x="90" y="48" width="150" height="64" rx="8" fill="#111827" stroke="#22d3ee" stroke-width="1.25"></rect>
          <text x="165" y="85" text-anchor="middle" fill="#67e8f9" font-size="11" font-family="var(--font-mono)"
            >Personal DX</text
          >
        </g>
        <g data-cm-cluster="agents" opacity="0.9">
          <rect x="285" y="36" width="150" height="64" rx="8" fill="#111827" stroke="#a78bfa" stroke-width="1.25"
          ></rect>
          <text x="360" y="73" text-anchor="middle" fill="#c4b5fd" font-size="11" font-family="var(--font-mono)"
            >Agentic</text
          >
        </g>
        <g data-cm-cluster="create" opacity="0.9">
          <rect x="480" y="48" width="150" height="64" rx="8" fill="#111827" stroke="#fb923c" stroke-width="1.25"
          ></rect>
          <text x="555" y="85" text-anchor="middle" fill="#fdba74" font-size="11" font-family="var(--font-mono)"
            >Create Awesome</text
          >
        </g>
        <g data-cm-cluster="v" opacity="0.9">
          <rect x="180" y="178" width="140" height="56" rx="8" fill="#111827" stroke="#5b9bd5" stroke-width="1.25"
          ></rect>
          <text x="250" y="211" text-anchor="middle" fill="#93c5fd" font-size="11" font-family="var(--font-mono)"
            >V world</text
          >
        </g>
        <g data-cm-cluster="incubating" opacity="0.9">
          <rect x="400" y="178" width="160" height="56" rx="8" fill="#111827" stroke="#f472b6" stroke-width="1.25"
          ></rect>
          <text x="480" y="211" text-anchor="middle" fill="#f9a8d4" font-size="11" font-family="var(--font-mono)"
            >Incubating</text
          >
        </g>
        <g transform="translate(360 140)">
          <rect
            x="-54"
            y="-18"
            width="108"
            height="36"
            rx="2"
            fill="color-mix(in srgb, var(--nest-midnight-900, #0f172a) 88%, transparent)"
            stroke="color-mix(in srgb, var(--nest-cyan, #22d3ee) 70%, transparent)"
            stroke-width="1.5"></rect>
          <text
            y="5"
            text-anchor="middle"
            fill="var(--nest-cyan, #22d3ee)"
            font-family="var(--font-mono, monospace)"
            font-size="11"
            font-weight="700"
            letter-spacing="0.14em">PLAZA</text
          >
        </g>
      </svg>

      <div class="cm-plaza__nodes" role="list" aria-label="Plaza stations">
        {
          nodes.map((node) => (
            <div role="listitem">
              <VisualNode
                id={node.id}
                label={node.name}
                pressed={node.id === defaultNode.id}
                disabled
                accent={node.kind === 'discord' ? communityMeta.discordBlurple : undefined}
              />
            </div>
          ))
        }
      </div>

      <div class="cm-plaza__incubating">
        <p class="cm-plaza__kicker">Incubating blueprints</p>
        <ul>
          {
            incubatingProjects.map((p) => (
              <li>
                <strong>{p.workingTitle}</strong>
                <span> · {p.themes.slice(0, 2).join(' · ')}</span>
              </li>
            ))
          }
        </ul>
      </div>

      <aside class="cm-plaza__static-index" data-cm-static-index aria-labelledby="cm-static-index-title">
        <p id="cm-static-index-title" class="cm-plaza__kicker">Static station index</p>
        <ul aria-label="Community stations">
          {
            nodes.map((node) => (
              <li>
                <strong>{node.name}</strong>
                <span>{node.summary}</span>
                <small>
                  {node.ecosystem} · {node.areas.join(' · ')}
                </small>
                <a
                  href={node.href}
                  target={node.href.startsWith('http') ? '_blank' : undefined}
                  rel={node.href.startsWith('http') ? 'noopener noreferrer' : undefined}
                >
                  {node.kind === 'discord' ? 'Join the Discord' : 'Open station'}
                </a>
              </li>
            ))
          }
        </ul>
      </aside>
    </div>

    <SceneInspector eyebrow="Station" title={defaultNode.name} id="cm-plaza-inspector" headingLevel={2}>
      <p data-cm-copy>{defaultNode.summary}</p>
      <Fragment slot="meta">
        <div class="cm-plaza__meta" data-cm-meta>
          <span data-cm-ecosystem>{defaultNode.ecosystem}</span>
          <span data-cm-areas>{defaultNode.areas.join(' · ')}</span>
          <a
            data-cm-link
            href={defaultNode.href}
            target={defaultNode.href.startsWith('http') ? '_blank' : undefined}
            rel={defaultNode.href.startsWith('http') ? 'noopener noreferrer' : undefined}
          >
            {defaultNode.kind === 'discord' ? 'Join the Discord' : 'Open'}
          </a>
        </div>
      </Fragment>
    </SceneInspector>
  </div>

  <p class="visually-hidden" role="status" aria-atomic="true" data-cm-status></p>

  <ContributionExplorer />

  <section class="cm-plaza__details">
    <h2 class="cm-plaza__label">Community basics</h2>
    <p>{communityMeta.metricsPolicy}</p>
    <p>Invite verified · discord.gg/bR5VyATgka · voluntary participation.</p>
    <h3 class="cm-plaza__basics-h">Support</h3>
    <ul>
      {
        supportChannels.map((channel) => (
          <li>
            <a
              href={channel.href}
              target={channel.external ? '_blank' : undefined}
              rel={channel.external ? 'noopener noreferrer' : undefined}
            >
              {channel.label}
            </a>
            — {channel.description}
          </li>
        ))
      }
    </ul>
    <h3 class="cm-plaza__basics-h">Moderation &amp; privacy</h3>
    <ul>
      {
        moderationItems.map((item) => (
          <li>
            <strong>{item.title}</strong> — {item.description}
          </li>
        ))
      }
    </ul>
    <p>
      CoC example:
      <a href={communityMeta.exampleCodeOfConductUrl} target="_blank" rel="noopener noreferrer"> CODE_OF_CONDUCT ↗ </a>
    </p>
  </section>

  <WorldDock label="Dock">
    <a href="#find-something">Find issues</a>
    <a href="/projects">Projects</a>
    <a href="/open-source">Open Source</a>
  </WorldDock>
</section>

<style>
  .cm-plaza {
    display: grid;
    gap: 1rem;
    width: min(100% - 2 * var(--section-padding-x, 1.5rem), 78rem);
    max-width: none;
    margin: 0 auto;
    padding: clamp(1.25rem, 3vw, 2.5rem) clamp(0.75rem, 2vw, 1.5rem) 2rem;
    border-inline: 1px solid color-mix(in srgb, var(--nest-sunset-orange) 20%, transparent);
    /* Workshop surface: faint pegboard grid over the midnight background. */
    background-image:
      linear-gradient(color-mix(in srgb, var(--nest-sunset-orange) 3%, transparent) 1px, transparent 1px),
      linear-gradient(90deg, color-mix(in srgb, var(--nest-sunset-orange) 3%, transparent) 1px, transparent 1px);
    background-size: 3.5rem 3.5rem;
  }

  @media (width >= 860px) {
    .cm-plaza {
      padding-inline: 1.5rem;
    }
  }

  .cm-plaza__header {
    display: grid;
    gap: 1rem;
    padding: clamp(1.25rem, 3vw, 2.75rem) clamp(0.75rem, 3vw, 2rem);
    border-block: 1px solid color-mix(in srgb, var(--nest-sunset-orange) 32%, transparent);
    border-inline-start: 2px solid var(--nest-sunset-orange);
    background: linear-gradient(90deg, color-mix(in srgb, var(--nest-sunset-orange) 7%, transparent), transparent 70%);
  }

  @media (min-width: 820px) {
    .cm-plaza__header {
      grid-template-columns: minmax(0, 1.25fr) minmax(17rem, 0.75fr);
      align-items: end;
    }
  }

  .cm-plaza__header-note {
    padding: 0.9rem 1rem;
    border: 1px solid color-mix(in srgb, var(--nest-cyan) 38%, transparent);
    background: color-mix(in srgb, var(--nest-midnight-950) 78%, transparent);
  }

  .cm-plaza__header-note p {
    margin: 0;
    color: var(--color-text-muted);
    font-size: 0.82rem;
    line-height: 1.45;
  }

  .cm-plaza__header-note .cm-plaza__kicker {
    margin-bottom: 0.65rem;
  }

  .cm-plaza__header-note a {
    display: inline-flex;
    min-height: 44px;
    align-items: center;
    margin-top: 0.55rem;
    color: var(--nest-cyan);
    font-family: var(--font-mono);
    font-size: 0.72rem;
    text-decoration: none;
  }

  .cm-plaza__eyebrow,
  .cm-plaza__kicker {
    margin: 0;
    font-family: var(--font-mono, ui-monospace, monospace);
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--world-accent, var(--nest-cyan, #22d3ee));
  }

  .cm-plaza__title {
    margin: 0;
    font-size: clamp(1.6rem, 3.5vw, 2.1rem);
    font-weight: 800;
    letter-spacing: 0.02em;
    text-transform: uppercase;
    color: var(--color-text);
  }

  .cm-plaza__lede {
    margin: 0;
    max-width: 56ch;
    color: var(--color-text-muted);
    font-size: 0.95rem;
    line-height: 1.45;
  }

  .cm-plaza__principles {
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem 0.75rem;
    margin: 0;
    padding: 0;
    list-style: none;
    font-family: var(--font-mono, ui-monospace, monospace);
    font-size: 0.7rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-text-muted);
  }

  .cm-plaza__principles li {
    padding: 0.35rem 0.55rem;
    border: 1px solid var(--color-border);
    clip-path: var(--clip-corner-sm);
  }

  .cm-plaza__scene {
    margin-top: 0.25rem;
  }

  .cm-plaza__stage {
    display: grid;
    gap: 0.75rem;
    padding: 0.85rem;
    min-height: 18rem;
  }

  .cm-plaza__svg {
    display: block;
    width: 100%;
    max-width: 40rem;
    height: auto;
    margin-inline: auto;
  }

  .cm-plaza__svg [data-cm-cluster] {
    opacity: 0.45;
    transition: opacity 160ms ease;
  }

  .cm-plaza__svg [data-cm-cluster].is-lit {
    opacity: 1;
  }

  .cm-plaza__basics-h {
    margin: 0.75rem 0 0.35rem;
    font-size: 0.78rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-text);
  }

  .cm-plaza__nodes {
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem;
  }

  .cm-plaza__incubating {
    padding-top: 0.65rem;
    border-top: 1px dashed var(--color-border);
  }

  .cm-plaza__incubating ul {
    display: flex;
    flex-wrap: wrap;
    gap: 0.35rem 1rem;
    margin: 0.35rem 0 0;
    padding: 0;
    list-style: none;
    color: var(--color-text-muted);
    font-size: 0.82rem;
  }

  .cm-plaza__incubating strong {
    color: var(--color-text);
    font-weight: 700;
  }

  .cm-plaza__static-index {
    margin-top: 0.35rem;
    padding: 0.9rem 1rem;
    border-block: 1px dashed color-mix(in srgb, var(--nest-cyan) 30%, transparent);
    color: var(--color-text-muted);
  }

  .cm-plaza__static-index[hidden] {
    display: none;
  }

  .cm-plaza__static-index > ul {
    display: grid;
    gap: 0.7rem;
    margin: 0.65rem 0 0;
    padding: 0;
    list-style: none;
  }

  .cm-plaza__static-index li {
    display: grid;
    gap: 0.2rem;
    padding-top: 0.55rem;
    border-top: 1px solid color-mix(in srgb, var(--nest-violet) 22%, transparent);
    font-size: 0.78rem;
  }

  .cm-plaza__static-index li:first-child {
    padding-top: 0;
    border-top: 0;
  }

  .cm-plaza__static-index strong {
    color: var(--color-text);
  }

  .cm-plaza__static-index span,
  .cm-plaza__static-index small {
    line-height: 1.4;
  }

  .cm-plaza__static-index small {
    color: var(--color-text-faint);
    font-family: var(--font-mono);
    font-size: 0.68rem;
  }

  .cm-plaza__static-index a {
    display: inline-flex;
    align-items: center;
    min-height: 44px;
    color: var(--nest-cyan);
    font-family: var(--font-mono);
    font-size: 0.72rem;
    font-weight: 700;
  }

  .cm-plaza__discord {
    display: flex;
    flex-wrap: wrap;
    gap: 0.65rem 1rem;
    align-items: center;
    padding: 0.85rem 1rem;
    border: 1px solid color-mix(in srgb, #5865f2 45%, var(--color-border));
    background: color-mix(in srgb, #5865f2 12%, transparent);
    clip-path: var(--clip-corner-sm);
    margin-top: 0.25rem;
  }

  .cm-plaza__discord-cta {
    display: inline-flex;
    align-items: center;
    min-height: 44px;
    padding: 0 1rem;
    background: #5865f2;
    border: 1px solid #5865f2;
    color: #fff;
    font-weight: 800;
    font-size: 0.9rem;
    text-decoration: none;
    clip-path: var(--clip-corner-sm);
  }

  .cm-plaza__discord-cta:hover {
    background: #4752c4;
    border-color: #4752c4;
  }

  .cm-plaza__discord-cta:focus-visible {
    outline: 2px solid var(--color-focus-ring);
    outline-offset: 2px;
  }

  .cm-plaza__discord span {
    flex: 1 1 16rem;
    color: var(--color-text-muted);
    font-size: 0.8rem;
    line-height: 1.4;
  }

  .cm-plaza__details {
    margin: 0;
    font-size: 0.86rem;
    color: var(--color-text-muted);
  }

  .cm-plaza__label {
    margin: 0 0 0.5rem;
    font-family: var(--font-mono, ui-monospace, monospace);
    font-size: 0.82rem;
    font-weight: 400;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-text);
  }

  .cm-plaza__details > p {
    margin: 0.45rem 0;
  }

  .cm-plaza__details > .cm-plaza__basics-h {
    margin-top: 1rem;
    color: var(--nest-cyan, #22d3ee);
  }

  .cm-plaza__details ul {
    margin: 0.5rem 0 0;
    padding-left: 1.1rem;
    line-height: 1.5;
  }

  .cm-plaza__details a {
    color: var(--nest-cyan, #22d3ee);
    font-weight: 700;
  }

  .cm-plaza__meta {
    display: grid;
    gap: 0.35rem;
  }

  .cm-plaza__meta [data-cm-link] {
    display: inline-flex;
    align-items: center;
    min-height: var(--touch-min, 44px);
    color: var(--nest-cyan, #22d3ee);
    font-weight: 700;
    text-decoration: none;
  }

  .cm-plaza__meta [data-cm-link][data-kind='discord'] {
    color: #c7d2fe;
  }
</style>

<script>
  type PlazaNode = {
    id: string;
    name: string;
    summary: string;
    areas: string[];
    ecosystem: string;
    href: string;
    kind: 'discord' | 'project';
  };

  function bindPlaza(root: HTMLElement) {
    const nodes = JSON.parse(root.dataset.nodes || '[]') as PlazaNode[];
    const titleEl = root.querySelector('#cm-plaza-inspector .vf-inspector__title');
    const copyEl = root.querySelector<HTMLElement>('[data-cm-copy]');
    const ecoEl = root.querySelector<HTMLElement>('[data-cm-ecosystem]');
    const areasEl = root.querySelector<HTMLElement>('[data-cm-areas]');
    const linkEl = root.querySelector<HTMLAnchorElement>('[data-cm-link]');
    const statusEl = root.querySelector<HTMLElement>('[data-cm-status]');
    const staticIndex = root.querySelector<HTMLElement>('[data-cm-static-index]');
    const buttons = root.querySelectorAll<HTMLButtonElement>('[data-visual-node]');

    const select = (id: string, announce = false) => {
      const node = nodes.find((n) => n.id === id);
      if (!node) return;
      buttons.forEach((btn) => {
        btn.setAttribute('aria-pressed', btn.dataset.visualNode === id ? 'true' : 'false');
      });
      if (titleEl) titleEl.textContent = node.name;
      if (copyEl) copyEl.textContent = node.summary;
      if (ecoEl) ecoEl.textContent = node.ecosystem;
      if (areasEl) areasEl.textContent = node.areas.join(' · ');
      if (linkEl) {
        linkEl.href = node.href;
        linkEl.textContent = node.kind === 'discord' ? 'Join the Discord' : 'Open';
        linkEl.dataset.kind = node.kind;
        if (node.kind === 'discord' || node.href.startsWith('http')) {
          linkEl.target = '_blank';
          linkEl.rel = 'noopener noreferrer';
        } else {
          linkEl.removeAttribute('target');
          linkEl.removeAttribute('rel');
        }
      }
      if (announce && statusEl) statusEl.textContent = `Station selected: ${node.name}.`;
      const eco = (node.ecosystem || '').toLowerCase();
      const cluster =
        node.kind === 'discord'
          ? 'agents'
          : eco.includes('v')
            ? 'v'
            : eco.includes('create')
              ? 'create'
              : eco.includes('dx') || eco.includes('dot') || eco.includes('workstation')
                ? 'dx'
                : eco.includes('incubating')
                  ? 'incubating'
                  : 'agents';
      root.querySelectorAll<SVGGElement>('[data-cm-cluster]').forEach((g) => {
        g.classList.toggle('is-lit', g.dataset.cmCluster === cluster);
      });
    };

    root.addEventListener('click', (e) => {
      const target = e.target;
      if (!(target instanceof HTMLElement)) return;
      const filter = target.closest<HTMLElement>('[data-cm-filter]');
      if (!filter) return;
      const id = filter.dataset.cmFilter || 'all';
      const map: Record<string, string> = {
        all: '',
        beginner: 'dx',
        'linux-desktop': 'dx',
        'ai-agents': 'agents',
        node: 'create',
        python: 'create',
        v: 'v',
        'devops-infra': 'incubating',
        documentation: 'agents',
        design: 'dx',
        testing: 'agents',
      };
      const cluster = map[id] ?? '';
      root.querySelectorAll<SVGGElement>('[data-cm-cluster]').forEach((g) => {
        g.classList.toggle('is-lit', !cluster || g.dataset.cmCluster === cluster);
      });
    });

    buttons.forEach((btn) => {
      btn.addEventListener('click', () => select(btn.dataset.visualNode || '', true));
    });

    const initial = Array.from(buttons).find((b) => b.getAttribute('aria-pressed') === 'true');
    if (initial?.dataset.visualNode) select(initial.dataset.visualNode);
    buttons.forEach((button) => {
      button.disabled = false;
    });
    if (staticIndex) staticIndex.hidden = true;
  }

  document.querySelectorAll<HTMLElement>('[data-cm-plaza]').forEach(bindPlaza);
</script>
