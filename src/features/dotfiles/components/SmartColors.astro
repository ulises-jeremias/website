---

---

<section class="smart-colors-stub" aria-labelledby="smart-colors-stub-title">
  <h2 id="smart-colors-stub-title">Smart Colors — wallpaper → palette → apps</h2>
  <div class="smart-colors-stub__flow" role="img" aria-label="Smart Colors flow: wallpaper to apps">
    <div class="smart-colors-stub__step">
      <div class="smart-colors-stub__box">Wallpaper</div>
      <span class="smart-colors-stub__arrow" aria-hidden="true">↓</span>
      <div class="smart-colors-stub__box">Material extraction</div>
      <span class="smart-colors-stub__arrow" aria-hidden="true">↓</span>
      <div class="smart-colors-stub__box">scheme.json cache</div>
      <span class="smart-colors-stub__arrow" aria-hidden="true">↓</span>
      <div class="smart-colors-stub__box">Quickshell / Hyprland / Kitty / GTK</div>
    </div>
    <p class="smart-colors-stub__caption">
      Maintained path: dots-wallpaper-set → pywal + generate-m3-colors.py → scheme.json → Colours service and desktop
      consumers. Prefer <code>SmartColorsAnimation</code> on <code>/dotfiles</code>.
    </p>
  </div>
</section>
<style>
  .smart-colors-stub {
    margin: 2rem 0;
  }
  .smart-colors-stub__flow {
    display: grid;
    place-items: center;
    gap: 1rem;
    padding: 1.25rem;
    border: 1px solid rgba(255, 176, 202, 0.18);
    background: rgba(45, 32, 39, 0.55);
    border-radius: 1rem;
  }
  .smart-colors-stub__step {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
  }
  .smart-colors-stub__box {
    padding: 0.6rem 0.9rem;
    border: 1px solid rgba(255, 176, 202, 0.22);
    background: #191114;
    color: #fbe7ec;
    border-radius: 0.55rem;
    font-size: 0.88rem;
    font-weight: 700;
  }
  .smart-colors-stub__arrow {
    color: #ffb0ca;
    font-weight: 800;
  }
  .smart-colors-stub__caption {
    max-width: 60ch;
    text-align: center;
    color: #d5c2c6;
    margin: 0;
    font-size: 0.9rem;
    line-height: 1.55;
  }
  .smart-colors-stub__caption code {
    color: #ffb0ca;
  }
</style>
